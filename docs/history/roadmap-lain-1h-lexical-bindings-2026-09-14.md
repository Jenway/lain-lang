# 编码 1h：局部绑定与参数更新切片

日期：2026-09-14。完整类型环境和 1h 尚未完成。

库 lowering 按最近词法作用域中已结束的声明解析局部引用，初始化表达式
沿外层绑定解析；声明与赋值共用类型输出。局部 IR 名称使用 AST 前序编号，
参数和可更新参数局部值各有独立编号。记录基址引用沿同一绑定解析，记录
字段 store 明示物理类型，避免窄字段按 64 位写入。

当前源码的 `check_local_assignment_types.py --deterministic` 退出 0：
15 项通过 verifier、实际返回 42、两次独立编译产物逐字节一致。覆盖块内
addr/bool、推导类型、后声明、初始化遮蔽、参数循环、记录布局遮蔽、宏
生成局部变量和参数名称冲突。旧原型分别有 verifier 2011 和运行结果 40
的反例，新编号修复后通过。

新编号原型的 46 文件编译器源码闭包已编译并通过 verifier（session 77520）。
合入源码另保留已验证的窗口节点身份修复，删除无消费者的旧条件扫描函数。
合入后 12 项条件/表达式确定性回归、签名、12 项宏展开、优先级实际运行
和 core/stdlib 边界检查通过。优先级文本断言改为完整乘加结构，不依赖旧
参数名；运行预期值 37 保留。

此检查已加入主门禁，共 44 项。完整门禁、正式库当前构建及更广的成员/
调用语义尚待验收；闭包 verifier 通过不证明正式编译器执行或固定点。
