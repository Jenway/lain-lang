# 编码 1h：宏复制节点的表达式窗口

日期：2026-09-14。仅归档表达式窗口边界修复，完整 1h 尚未完成。

`let inc = macro(x) { x + 1 };` 中 `return inc(20) + inc(20);`
原先生成 `#return #add(20, 1)`，通过 verifier 后预期值检查失败。
两次展开复制相同源码位置，窗口扫描把不同 AST 节点误当成终点。

库 lowering 的窗口扫描改用实际节点地址相等判断终点。其它使用源码
跨度比较的逻辑未在本切片更改，仍需按各自的合成节点契约核验。

当前源码重建后的 `check_condition_expression_lowering.py --deterministic`
退出 0：12 项通过 verifier、实际返回 42，两次独立编译输出逐字节一致。
含同样修复的局部绑定原型另通过 15 项运行及确定性回归；原型完整闭包
验证尚在运行，局部绑定修改尚未合入。此修复未改变 core/VM 语言规则。
