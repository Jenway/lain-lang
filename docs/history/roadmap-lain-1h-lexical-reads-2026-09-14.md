# 编码 1h：词法读取与诊断传播

日期：2026-09-14。完整类型环境和 1h 尚未完成。

普通值读取改用最近词法绑定，移除按单字母大写放行未声明值的分支。
嵌套 local/while/return 验证保留底层诊断；删除三个旧全函数局部扫描函数。
类型侧仍有独立的大写名及 payload 恢复逻辑，本切片未更改。

隔离版本的 46 文件完整源码闭包编译和 verifier 通过（session 51294）。
合入后 `check_local_assignment_types.py --deterministic --check-scope-errors`
退出 0：十五项正例运行到 42，独立编译产物一致，九项非法引用返回 5108。
显式 `X:i64` 参数的补测返回 42。此项已登记到现有主门禁，共 44 项。

此前完整门禁在 fixture IR 对比失败。对比现按过程统一绑定名，仅折叠
类型一致且没有后续赋值的参数副本；无注解整数常量 store 按 seed 推导
规则显式记为 bits<64>。参数顺序错误、可变副本及窄 store 差异应保留。
当前 fixture 对比仍在运行，完整门禁未重新通过。
