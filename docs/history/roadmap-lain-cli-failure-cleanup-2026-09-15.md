# 编译失败输出清理

`run_lain_compiler.py` 在编译执行失败和诊断文本输出两条路径中移除输出文件。
存在诊断文本时将其保留在 stderr，避免只报告外层退出状态而丢失上下文。

`check_compiler_artifact_verification.py` 三项通过：成功程序返回 42；无效
物理调用及非法声明均失败且无输出文件。输入行回归及十一项失败诊断通过，
`git diff --check` 通过。

导入收集错误的库层传播仍在隔离原型中，未由驱动清理代替；完整编译 API
失败生命周期与资源释放继续由当前路线图跟踪。
