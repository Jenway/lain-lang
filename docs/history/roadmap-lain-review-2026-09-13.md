# Roadmap 整理核验记录：2026-09-13

本记录补充既有快照，不修改历史验收结果。当前待办见 [路线图](../roadmaps/lain-roadmap.md)。

## 已实现部分的归属

编码 0、4 及编码 2/3 的部分 bootstrap 实现见 [完成归档](roadmap-lain-completed-2026-09-13.md)。
编码 1d 路径审计、1e 阶段交接、宏辅助迁移、默认宏展开及正式参数隔离，
1h 普通返回表达式、编码 7 负数除法与参数位宽修复，各自已有独立完成快照，统一由
[历史索引](README.md) 导航。完整阶段仍有未完成工作，未整体归档为完成。

## 从当前计划移出的执行记录

- effect 工厂显式导入 `std::effect`，使用 `effects.Effect` 返回及绑定类型的正例，
  bootstrap 编译、verifier、运行通过；main 仅返回常量 42，未消费 operation/handler。
- 相同依赖下仅把返回类型改为 `Module` 的反例仍被接受，证明库返回类型检查未完成。
- 旧无导入 fixture 加入 `std/effect.lain` 后返回 3101，依赖差异不能证明返回类型检查。
- 依赖形参类型与位宽已有运行覆盖，详见完成归档；完整 `Ord(T)` 策略正例仍待完成。

正式类型表达式语法的三个修复在隔离 ABI 的检查中通过 12 项对比，剩余 1 项 SKIP。
生产重建/default gate 尚未取得终态结果，保留为待验收，未归档为完成。

## 本次校验边界

检查当前路线图的本地链接、代码路径、现有命令与计划命令的区分，以及已完成阶段是否
仍出现在待办中。未重新运行全量编译器门禁，本记录不证明当前源码的全量运行正确性。

本次路径检查：路线图初次检查的 5 个链接、44 个源码/脚本路径均有效；4 个待实现检查
明确列为计划入口。最终补充的归档链接存在。`git diff --check` 通过。
