sub @main() -> i32 {
block_0:
  %agg = alloca(8)
  store 10, lea(base=%agg, offset=0)
  store 20, lea(base=%agg, offset=4)
  %a = field[0](%agg)
  %b = field[4](%agg)
  %sum = primitive integer.add(%a, %b)
  return %sum
}
