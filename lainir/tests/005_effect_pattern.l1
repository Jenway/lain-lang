#proc main() -> i32 {
block_0:
  %agg = #alloca(12)
  #store 0, #lea(base=%agg, offset=0)
  #store 42, #lea(base=%agg, offset=4)
  #store 0, #lea(base=%agg, offset=8)
  %disc = #field[0](%agg)
  %eq = #primitive integer.eq(%disc, 0)
  #cond_br %eq, block_ok, block_err
block_ok:
  %val = #field[4](%agg)
  #return %val
block_err:
  %err = #field[8](%agg)
  #return %err
}
