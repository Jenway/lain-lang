// test struct with constants
let W = 640;
let H = 480;

struct Rect {
    w: i32,
    h: i32,
}

fn area(r: Rect) -> i32 {
    r.w * r.h
}

fn main() -> i32 {
    let r = Rect { w: W, h: H };
    area(r)
}
