import std::dynarray;

struct Point { x: f64, y: f64 }

fn main() -> () ! {Alloc, RawPtrWrite} {
    let mut arr: DynArray<Point> = DynArray::new();
    let p = Point { x: 1.0_f64, y: 2.0_f64 };
    arr.push(p);
    let _n = arr.len();
    0;
}
