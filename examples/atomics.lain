fn publish(flag: &Rel<bool>) -> () {
    flag.store(true, Rel)
}

fn consume(flag: &Acq<bool>) -> bool {
    flag.load(Acq)
}

fn main() -> bool {
    let shared flag: bool = false;
    publish(&flag);
    consume(&flag)
}
