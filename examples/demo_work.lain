// Working demo: struct + field access + binary ops + function calls
struct Rect { w: i32, h: i32 }

fn perimeter(r: Rect) -> i32 {
    r.w + r.w + r.h + r.h
}

fn main() -> i32 {
    let r = Rect { w: 640, h: 480 };
    perimeter(r)
}
