// Struct construction, field access, arithmetic, and a function call.
let Rect: type = std::struct {
    w: i32,
    h: i32,
}

let perimeter = std::func(r: Rect) -> i32 {
    r.w + r.w + r.h + r.h
}

let main = std::func() -> i32 {
    let r = Rect { w: 640, h: 480 };
    perimeter(r)
}
