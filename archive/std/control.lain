@compiler_intrinsic(likely)
@export
let likely = std::func(condition: bool) -> bool;

@compiler_intrinsic(unlikely)
@export
let unlikely = std::func(condition: bool) -> bool;

@compiler_intrinsic(unreachable)
@export
let unreachable = std::func() -> !;
