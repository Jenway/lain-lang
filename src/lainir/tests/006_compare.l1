#proc eq(i32 %a, i32 %b) -> i1 {
block_0:
  %r = #primitive integer.eq(%a, %b)
  #return %r
}

#proc lt(i32 %a, i32 %b) -> i1 {
block_0:
  %r = #primitive integer.lt(%a, %b)
  #return %r
}

#proc main() -> i32 {
block_0:
  %a = #call eq(5, 5)
  %b = #call lt(3, 7)
  %sum = #primitive integer.add(0, 42)
  #return %sum
}
