# LAINIR capability contract.
#
# The contract is the interface a provider must satisfy; it is deliberately
# independent of any implementation.  The second Lain-written provider
# (l1_ir, l1_unit_builder, l1_verifier, l1_printer, default_provider) is
# paused and archived under docs/history/formal-implementations/ until Lain
# is mature enough to rewrite it.  src/lainc consumes only the contract
# types below, so it keeps compiling without a provider.
src/lainir/api_contract.lain
