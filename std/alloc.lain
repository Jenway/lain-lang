@foreign(c, link_name = "malloc")
pub fn malloc(size: usize) -> *mut u8 ! {Alloc};

@foreign(c, link_name = "realloc")
pub fn realloc(ptr: *mut u8, size: usize) -> *mut u8 ! {Alloc};

@foreign(c, link_name = "free")
pub fn free(ptr: *mut u8) -> () ! {Alloc};
