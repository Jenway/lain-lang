@foreign(c, link_name = "malloc")
pub fn malloc(size: usize) -> opaque ! {Alloc};

@foreign(c, link_name = "realloc")
pub fn realloc(ptr: opaque, size: usize) -> opaque ! {Alloc};

@foreign(c, link_name = "free")
pub fn free(ptr: opaque) -> () ! {Alloc};
