;; ===========================================================================
;; std/meta/pipeline/driver.scm — Pipeline 基础设施
;;
;; define-pass / __lain-passes 由 C 侧 (helpers.c) 注入。
;; 本文件现在是空壳 — 所有加载由顶层的 std/meta/driver.scm 负责。
;; ===========================================================================
