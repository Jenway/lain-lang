import std::effects::suspend;

pub effect Spawn {
    fn spawn(task: fn() -> () ! {Suspend}) -> () ! {Suspend};
    fn spawn_with(task: fn(usize) -> () ! {IO, Suspend}, data: usize) -> () ! {IO, Suspend}
}
