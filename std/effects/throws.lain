@try_abi(throws, throw = throw)
pub effect Throws<E> {
    fn throw(error: E) -> !
}

pub struct ThrowsToResult {}

@try_abi(to_result)
pub fn Throws::to_result() -> ThrowsToResult {
    ThrowsToResult {}
}
