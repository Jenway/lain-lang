let AstTree: type = std::struct {
    tag: i32,
    symbol: i32,
    left: i32,
    right: i32,
}

let TypeShape: type = std::struct {
    name: i32,
    arg: i32,
    arg_count: i32,
}

@export

let type_application_score = std::func(name_symbol: i32, arg_symbol: i32) -> i32 {
    let shape: TypeShape = TypeShape {
        name: name_symbol,
        arg: arg_symbol,
        arg_count: 1,
    };
    shape.name + shape.arg + shape.arg_count
}

@export

let type_application_from_ast_score = std::func(name_tag: i32, name_symbol: i32, arg_tag: i32, arg_symbol: i32) -> i32 {
    let name_node: AstTree = AstTree {
        tag: name_tag,
        symbol: name_symbol,
        left: 0,
        right: 0,
    };
    let arg_node: AstTree = AstTree {
        tag: arg_tag,
        symbol: arg_symbol,
        left: 0,
        right: 0,
    };
    type_application_score(name_node.symbol, arg_node.symbol)
}
