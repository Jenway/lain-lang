// Module with 3-param function, only export some via export block
let add3 = std::func(a: i32, b: i32, c: i32) -> i32 { a }
let mul3 = std::func(a: i32, b: i32, c: i32) -> i32 { b }
let not_exported = std::func(x: i32) -> i32 { x }

export {
    add3,
    mul3,
}
