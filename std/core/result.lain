// Explicit result data for algorithms whose failure is itself a value.
// Allocation, bounds policy and platform operations use effects instead.

@export
let result: Module = std::module {
    @export
    let Result = std::enum(
        T: type,
        E: type,
    ) {
        Ok(T),
        Err(E),
    };
};
