@foreign(c)
fn puts(message: CStr) -> i32 ! {IO};

@foreign(c)
fn printf(format: CStr, value: i32) -> i32 ! {IO};

fn print_line(message: CStr) -> i32 ! {IO} {
    puts(message)
}

fn print_i32(format: CStr, value: i32) -> i32 ! {IO} {
    printf(format, value)
}
