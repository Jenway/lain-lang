@foreign(c)
pub fn puts(message: CStr) -> i32 ! {IO};

@foreign(c)
pub fn printf(format: CStr, value: i32) -> i32 ! {IO};

pub fn print_line(message: CStr) -> i32 ! {IO} {
    puts(message)
}

pub fn print_i32(format: CStr, value: i32) -> i32 ! {IO} {
    printf(format, value)
}
