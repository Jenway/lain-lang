#data greeting(6, 1, "hello");

#data escapes(12, 4, "a\nb\t\\\"\x0f\q");

#proc len_of() -> #bits<32> {
  #return 5
}

#proc main() -> #bits<32> {
  #let %p: #addr = #data_addr(greeting)
  #let %q: #addr = #data_addr(escapes)
  #return #call len_of()
}
