#proc main() -> #bits<64> {
  #let %value: #bits<64> = #eval {
    #let %outer: #addr = #alloca(8)
    #let %nested: #bits<64> = #eval {
      #let %inner: #addr = #alloca(8)
      #return 42
    }
    #return %nested
  }
  #return %value
}
