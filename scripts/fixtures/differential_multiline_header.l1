#proc multiline(
  #addr %storage,
  #bits<8> %small,
  #bits<32> %middle,
  #bits<64> %large
) ->
#bits<64> {
  #store[#bits<32>] %middle, %storage
  #return #add(#zext[#bits<64>](%small), #add(#zext[#bits<64>](#load[#bits<32>](%storage)), %large))
}
#proc main() -> #bits<32> {
  #let %storage: #addr = #alloca(8)
  #return #trunc[#bits<32>](#call multiline(%storage, #trunc[#bits<8>](1), #trunc[#bits<32>](20), 21))
}
