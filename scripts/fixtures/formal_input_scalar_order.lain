let combine = std::func(base: i32) ?{first: i32, second: i32} -> i32 {
    return base + second;
};

let outer: Module = std::module {
    let first: i32 = 40;
    let second: i32 = 100;
    @export let answer = combine(1);
};

let main = std::func() -> i32 {
    return outer.answer;
};
