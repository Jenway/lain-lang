#proc main() -> #bits<64> {
  #let %base: #bits<64> = 40
  #let %value: #bits<64> = #eval {
    #let %nested: #bits<64> = #eval {
      #return #add(%base, 2)
    }
    #return %nested
  }
  #return %value
}
