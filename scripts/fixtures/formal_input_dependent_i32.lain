let idf = std::func(a: T) ?{T} -> T {
    return a;
};

let outer: Module = std::module {
    let T: std::type = i32;
    @export let got: i32 = idf(300);
};

let main = std::func() -> i32 {
    return outer.got;
};
