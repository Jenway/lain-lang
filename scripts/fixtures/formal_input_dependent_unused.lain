let bias = std::func() ?{T} -> i32 {
    return 41 + 1;
};

let outer: Module = std::module {
    let T: std::type = i32;
    @export let got = bias();
};

let main = std::func() -> i32 {
    return outer.got;
};
