let pick = std::func() ?{T} -> T {
    return 300;
};

let outer: Module = std::module {
    let T: std::type = i8;
    @export let got: i32 = pick();
};

let main = std::func() -> i32 {
    return outer.got;
};
