// Minimal import collection input.  This alone does not prove that an effect
// operation or handler is lowered: its purpose is to expose failures while
// materializing the real std::effect module.
let effects: Module = import("std::effect");

let main = std::func() -> i32 {
    return 42;
};
