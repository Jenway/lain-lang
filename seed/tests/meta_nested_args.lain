# 嵌套调用当实参：实参表只有一份固定缓冲区时，外层的第一个实参会被内层写掉
# （`add3(g(1), g(2), g(3))` 曾发成 `#call add3(3, %r4, %r5)`，验证器不报，算出来 6）。
func g(x: i32) -> i32 { return x + 1; }
func add3(a: i32, b: i32, c: i32) -> i32 { return a + b + c; }
let main: i32 = add3(g(1), g(2), g(3));
