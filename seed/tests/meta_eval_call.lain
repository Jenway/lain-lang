# 函数体里 `let y: i32 = eval g(1);` —— **编译期求值**的调用。
# v0 的标记是 `eval` 前缀，**不是** `#eval`：`#` 到行尾是注释（见 `std/lex.l1`）。
# 期望：折叠阶段把 `%y = #eval g(1)` 算成常量，跑出来 2。
func g(x: i32) -> i32 { return x + 1; }
func f() -> i32 { let y: i32 = eval g(1); return y; }
let main: i32 = f();
