# 局部变量：多条语句、复制、常量绑定、以及「结尾必须有 return」
func calc(n: i32) -> i32 {
  let zero: i32 = 0;
  let copy: i32 = zero;
  let step: i32 = copy + n;
  let twice: i32 = step + step;
  return twice;
}
let main: i32 = calc(5);
