# 名字是历史：这一条**以前**确实被拒（Meta 报 6，`bool` 的 op 表里没有 `<`）。
# 2026-09-21 落地「字面量自己的类型」（参考经典 C）之后它通了 —— 两个操作数都是裸
# 字面量，`bool` 查不到这个算子就退回字面量自己的 i32，发 `#slt[#bits<32>](1, 2)`，
# 结果是 1 位，绑 `bool` 正好。
#
# 留着它是因为它是那条规则最直接的证据：`b` 必须等于 1。发成 `#slt[#bits<1>](1, 2)`
# 的话 `2` 会被截成 0、结果是假 —— 验证器不报，值就是错的。
let b: bool = 1 < 2;
