# Lain 源码的初代 prelude —— 输入语言的 root 环境。
#
# 不带点的类型名（`i32`、`usize`…）在这里查。它注册的逻辑路径是
# `std/prelude.lain`，所以 `scalar` 声明就是「这个语言有哪些标量」的
# **唯一**出处——不再有一份手打的字节表躺在编译器里。
#
#   scalar <名字> = <repr> { "<符号>" = <物理算子> ... }
#
# repr 的四种写法对应 LAINIR 的四个类型构造子：
#   bits<N> / f<N> / vec<N> / addr
#
# op 表是「源码里的符号 → 要发的物理算子」。i32 和 u32 的 repr 一样，
# 差别全在 `"/"` 那一行——Meta 里没有一行写着「如果是 i32」。

scalar i32 = bits<32> { "/" = sdiv "+" = add }
scalar u32 = bits<32> { "/" = udiv "+" = add }
scalar i64 = bits<64> { }
scalar i8 = bits<8> { }
scalar u8 = bits<8> { }
scalar bool = bits<1> { }
scalar usize = bits<64> { }
scalar addr = addr { }
