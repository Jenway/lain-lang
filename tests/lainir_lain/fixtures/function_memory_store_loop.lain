let store_loop = std::func(destination: addr, length: usize) -> unit {
    let index: usize = 0;
    while index < length {
        std::store_bits(8, destination, 1);
        index = index + 1;
    }
};
