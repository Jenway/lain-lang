let store = std::func(destination: addr, value: i32) -> i32 {
    return value;
};

let loop_store = std::func(destination: addr, length: usize) -> unit {
    while 0 < length {
        store(destination, 1);
    }
    return 42;
};
