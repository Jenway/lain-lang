let Region: type = std::struct {
    data: addr,
    length: usize,
};

let copy_raw = std::func(destination: Region, source: Region, length: usize) -> unit {
    std::store_bits(8, destination.data, std::load_bits(8, source.data));
};
