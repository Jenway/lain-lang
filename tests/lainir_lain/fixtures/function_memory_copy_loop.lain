let copy = std::func(destination: addr, source: addr, length: usize) -> unit {
    let index: usize = 0;
    while index < length {
        std::store_bits(8, destination, std::load_bits(8, source));
        index = index + 1;
    }
};
