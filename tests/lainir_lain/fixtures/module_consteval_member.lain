let math: Module = std::module {
    let square = std::consteval(x: usize) -> usize {
        return x * x;
    };
};
let answer = math.square(6);
let main = std::func() -> i32 {
    return answer;
};
