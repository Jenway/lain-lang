let make = std::func(value: i32) -> Module {
    return std::module {
        let get = std::func() -> i32 {
            return value;
        };
    };
};

let built = make(42);

let main = std::func() -> i32 {
    return built.get();
};
