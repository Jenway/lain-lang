let square = std::consteval(x: usize) -> usize {
    return x * x;
};
let calc = std::consteval(a: usize, b: usize) -> usize {
    return square(a) + square(b);
};
let answer = calc(2, 3);
let main = std::func() -> i32 {
    return answer;
};
