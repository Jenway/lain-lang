@foreign(c, link_name = "core.execute-lainir-text!")
fn execute_l1_text(text: addr, entry: addr) -> i32;

fn main() -> i32 {
    // A missing return value must be rejected by parse/verify, not executed.
    execute_l1_text("#proc main() -> i32 { #return }", "main")
}
