let app: Module = std::module {
    @export
    let main = std::func() -> i32 {
        let selected: i32 = if 1 == 1 {
            42
        } else {
            0
        };
        return selected;
    };
};
