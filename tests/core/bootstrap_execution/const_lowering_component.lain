// Minimal Lain-written lowering component for the interpreter closure.
// It represents the final value selected for an i32 constant-return target.
let lowerconst = std::func(value: i32) -> i32 {
    value
}

let main = std::func(value: i32) -> i32 {
    lowerconst(value)
}
