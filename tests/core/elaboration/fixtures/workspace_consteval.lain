let app: Module = std::module {
    let double = std::func(value: i32) -> i32 {
        return value + value;
    };

    @export
    let main = std::func() -> i32 {
        return std::consteval(double(21));
    };
};
