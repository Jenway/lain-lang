let platform: Module = import("std::platform::api");

let load = std::func(
    API: Module,
    path: &API.Memory.String,
) -> API.SourceBuffer ! {
    API.SourceRead,
} {
    return perform API.read_source(path);
};
