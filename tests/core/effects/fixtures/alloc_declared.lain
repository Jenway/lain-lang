let arena: Module = import("std::core::arena");

let allocate_one = std::func(
    size: usize,
) -> arena.Block ! {
    arena.Policy.Effect,
} {
    return perform arena.Policy.allocate(size, 1);
};
