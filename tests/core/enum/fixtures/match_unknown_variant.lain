let Option = std::enum(T: type) {
    None,
    Some(T),
};

let unwrap = std::func(option: Option(i32)) -> i32 {
    return match option {
        Option(i32).Missing => 0,
        _ => 42,
    };
};
