let Option = std::enum(T: type) {
    None,
    Some(T),
};

let unwrap = std::func(option: Option(i32)) -> i32 {
    return match option {
        Option(i32).Some(value) => value,
        Option(i32).Some(other) => other,
        Option(i32).None => 0,
    };
};
