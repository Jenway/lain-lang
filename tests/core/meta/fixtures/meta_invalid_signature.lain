let invalid_attribute = std::meta(
    std::func(invocation: MetaInvocation) -> SyntaxList {
        return syntax_list_one(invocation.target_syntax);
    }
);

@invalid_attribute
let main = std::func() -> i32 {
    return 42;
};
