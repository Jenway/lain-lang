# Meta Core Tests

Pending suite.

Purpose: verify that domain parsers own language semantics.

Initial contracts:

- `fn/parse.scm` owns function declarations
- `struct/parse.scm` owns struct declarations
- `types/parse.scm` owns type application
- `effects/form.scm` owns effect application
- `import/parse.scm` owns import declarations
- `attrs` owns attributes
