#proc add(#bits<32> %left, #bits<32> %right) -> #bits<32> {
  #return #add(%left, %right)
}

#proc main() -> #bits<32> {
  #let %target: #addr = #proc_addr(add)
  #return #call_indirect[(#bits<32>, #bits<32>) -> #bits<32>](%target, 40, 2)
}
