#proc sum(i32 %left, i32 %right) -> i32 {
  #return #add(%left, %right)
}

#proc main() -> i32 {
  #let %first: i32 = #eval { #return #call sum(40, 2) }
  #let %second: i32 = #eval { #return #call sum(1, 2) }
  #return #add(%first, %second)
}
