// CHECK: sub @main() -> {product}
// CHECK: alloca(9)
// CHECK: store 0,
// CHECK: store 42,
// CHECK: store 0,
// CHECK: return %__agg

fn main() -> i32 ! {Throws<i32>} {
    42
}

// CHECK: sub @inner() -> {product}
// CHECK: alloca(9)
// CHECK: store 0,
// CHECK: store 42,
// CHECK: store 0,
// CHECK: return %__agg

fn inner() -> i32 ! {Throws<i32>} {
    42
}

// TODO: outer() with ? operator — needs Task 3 (? lowering)
