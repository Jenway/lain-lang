// CHECK: #add(%arg0, %arg1)
// CHECK: #gt(%arg0, %arg1)

fn infer_local(left: i32, right: i32) -> i32 {
    let sum = left + right;
    sum
}

fn infer_bool(left: i32, right: i32) -> bool {
    let larger = left > right;
    larger
}
