// CHECK: primitive integer.mul(%arg1, %arg2)
// CHECK: primitive integer.add(%arg0
// CHECK: primitive integer.lt(primitive integer.add

fn arithmetic(left: i32, mid: i32, right: i32) -> i32 {
    left + mid * right
}

fn compare(left: i32, mid: i32, right: i32) -> bool {
    left + mid < right
}
