// if expression: both branches return
// NOTE: currently blocked by parser bug — literal in return value is parsed as null.
// CHECK: sub @choose(i1 %arg0) -> i32

fn choose(flag: bool) -> i32 {
    if flag {
        return 1;
    } else {
        return 2;
    };
}
