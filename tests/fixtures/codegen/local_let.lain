// CHECK: add i32
// CHECK: mul i32
// CHECK: icmp ugt i32

fn local_math(left: i32, right: i32) -> i32 {
    let sum: i32 = left + right;
    sum * right
}

fn local_compare(left: i32, right: i32) -> bool {
    let negated: i32 = -left;
    negated > right
}
