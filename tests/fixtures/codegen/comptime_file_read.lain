// comptime file read test — verify host_read_file capability works
// CHECK: #eval probe(

import std::build;

comptime fn probe() -> i32 {
    let _ptr = std::build::host_read_file("std/build.lain");
    42
}

fn main() -> i32 {
    probe()
}
