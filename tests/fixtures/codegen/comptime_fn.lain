// comptime fn — compile-time evaluation
// CHECK: #eval add_five(10)
// CHECK-NOT: #call add_five

comptime fn add_five(x: i32) -> i32 {
    return x + 5;
}

fn main() -> i32 {
    return add_five(10);
}
