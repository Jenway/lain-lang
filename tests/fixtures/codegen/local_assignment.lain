// CHECK: %_t0 = primitive integer.add(%arg0, %arg1)
// CHECK: return primitive integer.sub(%_t0, %arg1)

fn reassign(left: i32, right: i32) -> i32 {
    let mut value: i32 = left + right;
    value = value - right;
    value
}
