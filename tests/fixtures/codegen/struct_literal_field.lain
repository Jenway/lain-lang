// CHECK: alloca(8)
// CHECK: field[1](%__agg_1)

struct Pair {
    left: i32;
    right: i32;
}

fn pick(left: i32, right: i32) -> i32 {
    let pair = Pair { left: left, right: right };
    pair.right
}
