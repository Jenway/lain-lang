// CHECK: alloca(8)
// CHECK: field[0]
// CHECK: field[1]
// CHECK: primitive integer.add

struct Point {
    x: i32,
    y: i32,
};

fn make_point() -> i32 {
    let p: Point = Point { x: 10, y: 20 };
    p.x + p.y
}
