// tests/fixtures/check-pass/enum_simple.lain
// 验证 enum 声明 → tagged union 降级 → variant 构造函数可调用

enum Color {
    Red,
    Green,
    Blue
}

fn main() -> i32 {
    let c: Color = Color::Red();
    0
}
