// tests/fixtures/check-pass/dyn_dispatch2.lain
// 验证: 不在 tail 位置调用 — 简化调试

interface Animal {
    fn speak(self: addr) -> i32;
}

fn Dog_speak(data: addr) -> i32 {
    42
}

impl Animal for Dog {
    fn speak(self: addr) -> i32 { 42 }
}

fn test_dispatch(animal: Animal_Dyn) -> i32 {
    animal.speak();
    0
}

fn main() -> i32 {
    0
}
