(module math
  (format lci-v1)
  (exports
    (fn
      (name mul)
      (params (i32 i32))
      (ret i32)
      (link_name "math_mul"))
    (fn
      (name add)
      (params (i32 i32))
      (ret i32)
      (link_name "math_add"))
  ))
