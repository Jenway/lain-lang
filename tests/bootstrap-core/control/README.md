# Bootstrap Core Control

Pending tests for compiler control flow:

```text
match_nested.lain
early_return_result.lain
list_traversal.lain
```

These cases protect parser and lowerer implementation patterns.

