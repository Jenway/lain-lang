# Bootstrap Core Comptime

Pending tests for compile-time execution through LAIN-IR:

```text
comptime_build_ast.lain
comptime_parse_type.lain
comptime_read_fixture.lain
```

These cases protect self-hosting helpers that must run during compilation.

